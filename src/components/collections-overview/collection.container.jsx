import { gql } from "@apollo/client"
import { Query } from "@apollo/client"
const GRAPHQL_QUERY = gql``

const CollectionContainer = () => <Query query={}></Query>

export default collectionContainer
